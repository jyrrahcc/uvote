
// Re-export from the hooks folder
import { useToast, toast, type ToasterToast, type ToastActionElement } from "@/hooks/use-toast";

export { useToast, toast, type ToasterToast, type ToastActionElement };
